<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyView"
        tools:targetApi="31">
        <activity
            android:label="简易输入框"
            android:name=".used.EasyEditTextActivity"
            android:exported="false" />
        <activity
            android:label="计时文字"
            android:name=".used.TimingTextActivity"
            android:exported="false" />
        <activity
            android:label="圆环进度条"
            android:name=".used.ProgressBarActivity"
            android:exported="false" />
        <activity
            android:label="Mac地址输入框"
            android:name=".used.MacAddressActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>